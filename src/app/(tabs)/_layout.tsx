import React from 'react';
import AppTabs from '@/components/app-tabs';

/**
 * Tab Navigation Layout Router.
 * Mounts the custom NativeTabs and Tabs.web navigators for index, scan, explore, and settings tabs.
 */
export default function TabsLayout() {
  return <AppTabs />;
}
